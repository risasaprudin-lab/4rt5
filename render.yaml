services:
  - type: web
    name: macropulse-terminal
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: APP_URL
        sync: false
      - key: NODE_ENV
        value: production
